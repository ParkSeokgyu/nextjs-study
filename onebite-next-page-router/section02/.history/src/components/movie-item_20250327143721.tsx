export default function MovieItem({
  id,
  title,
  subTitle,
  description,
  releaseDate,
  company,
  genres: string[];
  runtime,
  posterImgUrl,
}: {MovieData}) {
  return (
    <div>도서 아이템</div>
  )
}